apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "echo-hostname.fullname" . }}-test-connection"
  labels:
    {{- include "echo-hostname.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "echo-hostname.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
